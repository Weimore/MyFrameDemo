<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.projectmodule">

    <application
        android:name=".utils.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".HomeActivity"
            android:launchMode="singleTask" />
        <activity android:name=".WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ProfileActivity" />
        <activity android:name=".testactivity.SampleListActivity" />
        <activity android:name=".testactivity.SampleFragmentActivity" />
        <activity android:name=".testactivity.SampleSectionListActivity" />
        <activity android:name=".testactivity.SampleTabActivity" />
        <activity android:name=".testactivity.SamplePagerActivity" />
    </application>

</manifest>